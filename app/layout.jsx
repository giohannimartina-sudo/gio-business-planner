import './globals.css';

export const metadata = {
  title: 'GIO Business Planner',
  description: 'GIO Business Planner',
};

export default function RootLayout({ children }) {
  return <html lang="nl"><body>{children}</body></html>;
}
